language: python
python:
  - "3.5"
  - "3.6"

install:
  - "pip install pipenv"
  - "pipenv install --dev"

script:
  - "python setup.py test"
#  - "pytest --doctest-modules"
  - "python -m nltk.downloader all"
  